'use client';

import React from 'react';
import JobForm from '@/components/forms/job';

export default function AddJob() {
    return (
        <JobForm
            data={null}
            isLoading={false}
        />
    );
}