'use client';

import React from 'react';
import MasterServiceForm from '@/components/forms/master-service';

export default function AddMasterService() {
    return (
        <MasterServiceForm
            data={null}
            isLoading={false}
        />
    );
}